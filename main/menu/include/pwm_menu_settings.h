#ifndef PWM_MENU_SETTINGS_H
#define PWM_MENU_SETTINGS_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

typedef struct pwm_menu_handle pwm_menu_handle_t;

void pwm_menu_settings_init(pwm_menu_handle_t *handle);

#endif /* PWM_MENU_SETTINGS_H */
