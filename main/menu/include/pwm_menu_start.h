#ifndef PWM_MENU_START_H
#define PWM_MENU_START_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdio.h>

typedef struct pwm_menu_handle pwm_menu_handle_t;

void pwm_menu_start_init(pwm_menu_handle_t *handle);

#endif /* PWM_MENU_START_H */